apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitness-chart.fullname" . }}-mongodb-service
  labels:
  {{- include "fitness-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.mongodbService.type }}
  selector:
    app: mongodb
    {{- include "fitness-chart.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.mongodbService.ports | toYaml | nindent 2 }}
