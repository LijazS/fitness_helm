apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitness-app.fullname" . }}
  labels:
    {{- include "fitness-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fitness-app.selectorLabels" . | nindent 4 }}
