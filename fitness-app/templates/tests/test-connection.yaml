apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fitness-app.fullname" . }}-test-connection"
  labels:
    {{- include "fitness-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fitness-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
